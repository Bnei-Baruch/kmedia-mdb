import React from 'react';
import PlaylistCollectionContainer from '../../Pages/PlaylistCollection/Container';

const EventCollection = () => <PlaylistCollectionContainer />;

export default React.memo(EventCollection);
