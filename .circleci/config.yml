version: 2
jobs:
  build:
    docker:
      - image: buildpack-deps:trusty
    steps:
      - checkout
      - run: yarn
  test:
    steps:
      - checkout
      - run: yarn
      - run: yarn test
workflows:
  version: 2
  test:
    jobs:
      - test
