version: 2
jobs:
  build:
    steps:
      - checkout
      - run: yarn
  test:
    steps:
      - checkout
      - run: yarn
      - run: yarn test
workflows:
  version: 2
  test:
    jobs:
      - test
