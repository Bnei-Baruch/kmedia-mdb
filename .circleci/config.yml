version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: yarn --ignore-engines
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: yarn --ignore-engines
      - run: yarn test
workflows:
  version: 2
  test:
    jobs:
      - test
